#include <iostream>
#include <array>

int main()
{
  const size_t max_length {80};

  char geniuses[][max_length] {
    "Elon Musk", "Donald Hoffman",
      "Dennis Ritchie", "Bjarne Stroustrup",
      "Johny Depp", "Leonardo Di Caprio"
      };

  if (choice>=1 && choice<= std::size(geiuses))
    {
      std::cout << "Your genius is: " << start[choice-1] << std::endl;
    }
  else
    {
      std::cout << "Sorry, you have'n got a genius." << std::endl;
    }
}
